# API Environment Variables
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# Run the setup script first to configure GCP authentication:
#   ./scripts/setup-local-dev.ps1 -ProjectId your-project-id   # Windows
#   ./scripts/setup-local-dev.sh your-project-id                # Linux/macOS

# GCP project ID for Firestore (your GCP project ID)
GOOGLE_CLOUD_PROJECT=your-project-id

# Recipe scraping Cloud Function URL
# Use localhost if running the function locally (./scripts/run-function.sh),
# or the deployed URL from: gcloud functions describe scrape_recipe --format='value(url)'
SCRAPE_FUNCTION_URL=http://localhost:8080

# Google Cloud Storage bucket for recipe images
# This is created by Terraform — check terraform output or GCP Console > Cloud Storage
GCS_BUCKET_NAME=your-project-id-recipe-images

# CORS allowed origins for local development
# These are the default Expo dev server ports — no need to change
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:8085,http://localhost:19006,http://localhost:3000

# Skip Firebase authentication for local development
# The app runs with a mock user (dev@localhost) — no Firebase setup needed
SKIP_AUTH=true

# Skip user allowlist check (allows any authenticated user)
# SKIP_ALLOWLIST=true

# Dev household ID (required when SKIP_AUTH=true to scope meal plans/grocery lists)
# Create a test household in Firestore, or leave empty to only work with recipes
# DEV_HOUSEHOLD_ID=

# Gemini API key for AI recipe enhancement (optional)
# Get a free key from: https://aistudio.google.com/apikey
# GOOGLE_API_KEY=your-gemini-api-key
